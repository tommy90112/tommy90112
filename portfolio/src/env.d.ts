/// <reference types="vite/client" />

// Brings in `import.meta.env` (BASE_URL, MODE, …) and the module declarations
// for Vite's asset imports. Declared here rather than via tsconfig `types` so
// it stays visible next to the code that relies on it.
